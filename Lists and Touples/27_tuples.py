touple=(2,4,3,6,)                                                               #in touples we can not update the values
print(touple[0])
touple = ()
print(touple)
touple=(25,)                                              # we should keep comma after values so taht we can its class type as touple
print(touple)
print(type(touple))





# touple[0]=34
# print(touple)
