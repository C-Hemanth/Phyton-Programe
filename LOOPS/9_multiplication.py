h=5
# h=int(input("Enter the number :\n"))                          # for input user
for i in range(10,0,-1):                                      # for reverse order of multiplication 
# for i in range(11):                                                # for normal order of multiplication
    # i=i+1                                                      # if we include i=i+1, the value of 11 is also is multiplied by 5
    print(str(h) + "X" + str(i) + "=" + str(h*i))                # we can use either "X" or "*" for multiplication
    # print(f"{h}*{i}={h*i}")                                        # or we can write like this as variables in bracket
    
                                    
    